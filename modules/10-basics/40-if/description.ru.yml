---
name: If
theory: |

  ```ruby
  def leap_year?(year)
    # % – остаток от деления
    if year % 4 == 0
      if year % 100 == 0
        if yearVar % 400 == 0
          # return нужен, иначе продолжится выполнение
          return true
        end
        return false
      end
      return true
    end
    # return не нужен
    false
  end
  ```

  ```ruby
  def leap_year?(year)
    (year % 4 == 0) && !(year % 100 == 0) || (year % 400 == 0)
  end
  ```

  1. Предикат (именование)
  1. if
  1. логические операторы


instructions: |

  Задание

tips: []
