---
name: Циклы
theory: |

  ```ruby
  def capitalize_words(sentence)
    result = ''
    1.upto(sentence.length) do
    end
  end
  ```

  ```ruby
  def leap_year?(year)
    (year % 4 == 0) && !(year % 100 == 0) || (year % 400 == 0)
  end
  ```

  1. Предикат (именование)
  1. if
  1. логические операторы


instructions: |

  Задание

tips: []
